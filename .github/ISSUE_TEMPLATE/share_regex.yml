name: Share Regex
description: Share your regex rules
labels: [share regex]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for sharing. Please use the followinng title format: |
          [Country] Description |
        For example: |
          [France] virtual numbers

  - type: textarea
    id: detail
    attributes:
      label: The regex
    validations:
      required: true

